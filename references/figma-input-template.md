# Figma 页面输入整理模板

> 用途：把 Figma 页面、节点、截图观察结果与人工补充说明整理成一份可继续喂给 AI 的高保真规格输入。
>
> 填写规则：
>
> - 设计原值直接写数值或样式
> - 不确定的内容写 `【待确认】`
> - 非设计直接给出、但为了推进开发所做的补全写 `【推断值】`
> - 若某个值来自人工修正而非 Figma 当前稿，请显式标记“人工修正”

## 1. 页面基础信息

- 页面名称：
- 页面用途：
- 所属业务：
- 目标端：
  - PC / 移动端 / 双端适配
- 目标输出：
  - 页面规格说明 / 页面代码 / 二者都需要

## 2. Figma 来源信息

- Figma 链接：
- 文件名称：
- 页面名称：
- 节点 ID：
- 节点路径：
- 关联截图或切图说明：
- 参考版本说明：

## 3. 页面整体信息

- 页面整体尺寸：
- 可视区域尺寸：
- 页面背景：
- 页面滚动方式：
  - 整页滚动 / 局部滚动 / 固定头部 / 固定底部 / 其他
- 页面整体布局方式：
  - 左右布局 / 上下布局 / 宫格布局 / 其他
- 是否依赖项目主布局：
- 是否存在固定操作栏：
- 是否存在右侧辅助区 / 导航区 / 指引区：
- 响应式策略概述：

## 4. 画板归类与状态识别

如果当前 Figma 节点下包含多个子画板、多个 frame、多个状态稿，请先填写本节，再继续写模块树。

### 4.1 总览信息

- 当前节点下是否包含多个画板 / 多个状态：
- 默认态画板：
- 是否包含浮层：
- 浮层类型：
  - modal / dialog / drawer / popup / popover / tooltip / dropdown
- 是否包含局部状态稿：
  - hover / active / focus / disabled / empty / loading / selected / expand / collapse / 其他
- 是否包含独立页面：
- 各画板归类结果：

示例：

```text
- Frame A：主页面默认态
- Frame B：主页面 + 打开弹窗态
- Frame C：主页面 + 下拉 popup 展开态
- Frame D：卡片 hover 态
- Frame E：空态示意
```

### 4.2 逐画板记录

以下内容请为每个子画板或状态稿单独复制一份。

- 画板 / 子节点名称：
- 归类：
  - 主页面默认态 / 同页状态稿 / 浮层态 / 局部组件状态稿 / 独立页面 / 说明参考稿
- 每个状态影响范围：
  - 页面整体 / 模块局部 / 组件局部
- 是否进入首轮开发：
- 是否作为默认态渲染：
- 浮层挂载归属：
- 触发来源元素：
- 遮罩层是否阻断底层交互：
- 是否仅作设计说明参考：
- 备注：

## 5. 页面模块树

请按层级列出页面模块树，例如：

```text
页面根节点
├─ 页面头部
├─ 内容区
│  ├─ 左侧主表单
│  │  ├─ 卡片 A
│  │  ├─ 卡片 B
│  │  └─ 卡片 C
│  └─ 右侧指引区
└─ 底部操作栏
```

实际模块树：

```text
[在这里填写]
```

## 6. 全局依赖

- 主布局规则：
- 全局 Header / Sidebar 依赖：
- 全局间距 / 色板 / 主题规则：
- 设备类型判断规则：
- 全局提示组件依赖：
- 全局表单控件规则：
- 路由或权限相关约束：

## 7. 公共组件依赖

- 组件库：
  - 例如：Arco Design
- 已有公共组件：
- 需要复用的全局弹窗 / 抽屉 / 选择器：
- 需要复用的全局表单包装：
- 需要复用的列表 / 卡片 / 时间轴 / 上传组件：
- 需要重点覆盖默认样式的组件：

## 8. 模块详细规格

以下内容请为每个模块单独复制一份。

---

### 模块名称

- 模块名称：
- 模块路径：
- 模块角色：
  - 主内容 / 辅助内容 / 操作区 / 导航区 / 浮层 / 其他
- 所属状态：
  - 默认态 / 某状态稿 / 浮层态 / `【待确认】`
- 数据来源：
  - Figma / 截图观察 / 人工说明 / 人工修正 / `【推断值】`

#### 8.1 结构

- 父子层级关系：
- 内部模块划分：
- 是否有标题栏：
- 是否有内容区：
- 是否有底部操作区：
- 是否有空态 / 加载态 / 禁用态 / 错误态：
- 所属横向容器角色判断：
  - 单子元素铺满型 / 左主体右操作型 / 左固定右铺满型 / 左工具位中间居中右工具位 / 左固定中间主体右操作 / 三列均衡型 / `【待确认】`

#### 8.2 尺寸与定位

- 宽度：
- 高度：
- 最小宽度 / 最大宽度：
- 最小高度 / 最大高度：
- 是否铺满父容器：
- X 坐标：
- Y 坐标：
- 是否依赖精确定位：
- 定位参考容器：
- z-index：
- 是否建立定位上下文：

#### 8.3 间距

- 外边距：
- 内边距：
- 模块间距：
- 行间距：
- 列间距：
- 标题与内容间距：
- 是否存在小数间距：

#### 8.4 字体与文本排版

- 字体名称（仅参考，不默认落地）：
- 实际落地字体策略：
  - 项目字体 / 全局字体栈 / 系统安全字体 / 已授权指定字体 / `【待确认】`
- 标题字号 / 字重 / 行高 / 颜色：
- 正文字号 / 字重 / 行高 / 颜色：
- 辅助文字字号 / 字重 / 行高 / 颜色：
- 标签文字样式：
- 按钮文字样式：
- letter-spacing：
- 文本对齐方式：
- 文本容器高度：
- 垂直对齐方式：
- 是否单行：
- 是否允许换行：
- 超出处理方式：
- 行数限制：
- 是否需要省略：
- 说明：
  - Figma 中的字体名称仅记录为参考，不默认落地
  - 实际开发时默认以前端项目字体方案为准
  - 若字体已授权且项目明确可用，可在补充说明中单独标注

#### 8.5 颜色

- 主文字颜色：
- 次文字颜色：
- 背景颜色：
- 边框颜色：
- 状态色：
- 图标颜色：

#### 8.6 边框与圆角

- 边框宽度：
- 边框样式：
- 边框颜色：
- 圆角：
- 是否有阴影：
- 阴影值：

#### 8.7 背景

- 纯色 / 渐变 / 图片 / 透明：
- 背景值：
- 是否叠加遮罩：
- 遮罩颜色：
- 遮罩覆盖范围：

#### 8.8 布局方式与宽度策略

- 布局方向：
  - 左右 / 上下 / 换行 / 宫格 / 绝对定位 / 固定定位
- 主轴对齐：
- 交叉轴对齐：
- 是否允许换行：
- 是否依赖绝对定位：
- 宽度性质：
  - 固定值 / 铺满 / 自适应 / 最小宽度 / 最大宽度 / 比例分配 / `【待确认】`
- 是否允许拉伸：
- 是否允许收缩：
- 最终宽度由谁决定：
  - 内容 / 父容器 / 剩余空间 / 设计固定值 / `【待确认】`

#### 8.9 交互说明

- 点击行为：
- 悬浮行为：
- 聚焦行为：
- 选中行为：
- 展开 / 收起行为：
- 删除 / 新增行为：
- 与其他模块的联动关系：
- 是否影响其他状态稿：

#### 8.10 显隐规则

- 默认是否展示：
- 在什么条件下展示：
- 在什么条件下隐藏：
- 是否受权限影响：
- 是否受设备类型影响：
- 是否属于首轮开发：

#### 8.11 表单与校验

- 是否包含表单控件：
- 控件类型：
- 必填项：
- 校验规则：
- 联动规则：
- 默认值：
- 重置规则：

#### 8.12 响应式差异

- PC 端表现：
- 移动端表现：
- 列数变化：
- 显隐变化：
- 尺寸变化：
- 布局方向变化：

#### 8.13 补充说明

- 素材路径：
- 图标来源：
- 文案约束：
- 业务规则：
- 是否滚动容器：
- 滚动方向：
- 固定区与滚动区边界：
- 小数尺寸处理建议：
- 风险点：

#### 8.14 定位归属补充

- 视觉归属：
- DOM 挂载归属：
- 定位参考归属：
- 是否脱离普通文档流：
- 是否覆盖在父容器内容之上：
- 是否需要正文内容预留空间：
- 是否影响父容器滚动：
- 推荐定位方式：
  - 普通流 / absolute / fixed / sticky / `【待确认】`
- 说明：
  - 这些字段适用于：表单底部操作栏、顶部吸附筛选栏、卡片角标、右上角操作区、浮在内容上的按钮栏、弹窗底部按钮区、局部吸底工具条

---

## 9. 页面级交互与状态补充

- 页面初始化行为：
- 数据加载顺序：
- 依赖选择顺序：
- 固定操作栏行为：
- 提交 / 重置 / 保存草稿逻辑：
- 弹窗 / 抽屉打开关闭逻辑：
- 全局提示规则：
- 默认态与状态态切换方式：
- 首轮交付范围说明：

## 10. 待确认项

- 待确认项 1：
- 待确认项 2：
- 待确认项 3：

## 11. 推断值清单

- 项目：
- 当前写法：
- 推断依据：
- 是否会影响还原度：

## 12. 布局意图补充

- 当前页面设计稿基准尺寸：
- 页面是否需要从固定尺寸适配到其他宽高：
- 哪些模块必须固定宽高：
- 哪些模块允许铺满：
- 哪些模块应内容自适应：
- 哪些横向容器应按“左主体右操作”处理：
- 哪些横向容器应按“左固定右铺满”处理：
- 哪些横向容器应按“中间视觉居中”处理：
- 哪些三子元素容器不能做三等分：
- 哪些区域允许滚动：
- 哪些区域禁止滚动：
- 哪些高度值允许取整：
- 哪些高度值不建议取整：
- 小数尺寸的处理建议：
- 若取整，优先向上 / 向下 / 保留原值：
- 哪些文本区需要最大宽度限制：
- 哪些文本区需要省略策略：
- 其他补充判断：

## 13. 多 node-id 结构关系补充

- 本次是否一次提供多个 node-id：
- 是否存在共同父节点：
- 页面根节点：
- 一级模块节点：
- 二级模块节点：
- 哪些节点属于同一父容器：
- 哪些节点属于同一模块内部：
- 哪些节点只是局部组件：
- 哪些节点是浮层：
- 哪些节点是状态稿：
- 哪些节点是独立页面：
- 各 node-id 之间的父子关系说明：
- 各 node-id 之间的兄弟关系说明：
- 各容器的布局方式：
- 哪些容器是滚动区：
- 哪些容器是固定区：
- 哪些关系仍待确认：

提示：

- 如果一次输入多个 node-id，请优先补充它们的结构关系，而不是只补每个节点的尺寸和样式
